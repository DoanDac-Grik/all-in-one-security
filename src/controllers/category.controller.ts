import { Request, Response, NextFunction } from 'express';

class CategoryController {
  detail(_req: Request, _res: Response, _next: NextFunction): void {
    // TODO: implement
  }
}

export default new CategoryController();
