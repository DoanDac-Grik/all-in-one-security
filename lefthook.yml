pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "src/**/*.ts"
      run: npx eslint --fix {staged_files}
    prettier:
      glob: "src/**/*.ts"
      run: npx prettier --write {staged_files}
    re-stage:
      run: git add {staged_files}
